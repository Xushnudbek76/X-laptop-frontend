VITE_API_URL=/api
FRONTEND_PORT=80
BACKEND_UPSTREAM=http://x-laptop-backend:3003

# If your backend is on a different domain instead of the same VPS/domain via Nginx proxy,
# use the full URL instead, for example:
# VITE_API_URL=https://api.example.com
